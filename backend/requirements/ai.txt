# ─────────────────────────────────────────────────────────────
#  IA · Groq es el ÚNICO proveedor externo
#
#  El LLM y la transcripción (Whisper) se consumen por HTTP con `requests`
#  (ya presente en base.txt): no hace falta ningún SDK de proveedor.
# ─────────────────────────────────────────────────────────────

# ── Orquestación del agente tutor ────────────────────────────
langgraph==0.2.60

# ── Embeddings LOCALES ───────────────────────────────────────
# Se ejecutan dentro del worker: ninguna llamada externa.
sentence-transformers==3.3.1

# ── Base vectorial ───────────────────────────────────────────
faiss-cpu==1.9.0.post1
numpy==1.26.4

# ── Procesamiento de audio/video ─────────────────────────────
# Extrae y trocea el audio antes de enviarlo a Groq Whisper.
ffmpeg-python==0.2.0

# ── Extracción de documentos ─────────────────────────────────
PyMuPDF==1.25.1                   # PDF
python-docx==1.1.2                # DOCX
python-pptx==1.0.2                # PPTX
pdf2image==1.17.0
pytesseract==0.3.13               # OCR de PDFs escaneados
markdown-it-py==3.0.0

# ── Tokenización ─────────────────────────────────────────────
tiktoken==0.8.0
